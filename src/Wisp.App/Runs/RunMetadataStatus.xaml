<UserControl x:Class="Wisp.App.Runs.RunMetadataStatus"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel LastChildFill="True" ToolTip="{Binding MetadataSaveHelp}">
        <Button Content="Retry save" Command="{Binding RetryMetadataSaveCommand}" DockPanel.Dock="Right" Padding="10,5" MinHeight="30" Margin="10,0,0,0" FontSize="12" AutomationProperties.Name="Retry saving run details">
            <Button.Style><Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                <Setter Property="Visibility" Value="Collapsed" />
                <Style.Triggers><DataTrigger Binding="{Binding HasMetadataSaveError}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers>
            </Style></Button.Style>
        </Button>
        <TextBlock Text="{Binding MetadataSaveStatus}" FontSize="11" TextWrapping="Wrap" VerticalAlignment="Center" AutomationProperties.LiveSetting="Polite">
            <TextBlock.Style><Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                <Setter Property="Foreground" Value="{DynamicResource MutedBrush}" />
                <Style.Triggers><DataTrigger Binding="{Binding HasMetadataSaveError}" Value="True"><Setter Property="Foreground" Value="{DynamicResource WarningBrush}" /></DataTrigger></Style.Triggers>
            </Style></TextBlock.Style>
        </TextBlock>
    </DockPanel>
</UserControl>
